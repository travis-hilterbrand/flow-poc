import { grey } from "@mui/material/colors";

// TODO - replace this with a real design system

export const getTextColor = () => grey[700];

export const getFontFamily = () =>
  "Roboto, Inter, system-ui, Avenir, Helvetica, Arial, sans-serif";
